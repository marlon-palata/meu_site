/* Reset Básico */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

/* Estilos Globais */
body {
    font-family: 'Helvetica Neue', sans-serif; /* Ou outra fonte importada */
    background-color: #f4f7f6; /* Fundo cinza claro */
    color: #333; /* Cor de texto principal */
    line-height: 1.6;
}

/* Cabeçalho */
header {
    background-color: #2c3e50; /* Azul escuro */
    color: white;
    text-align: center;
    padding: 20px 0; /* Shorthand: 20px topo/baixo, 0 esquerda/direita */
}

header h1 {
    font-size: 2.5rem; /* Usando rem */
    margin-bottom: 0.5rem; /* Usando rem */
}

/* Navegação */
header nav ul {
    list-style: none; /* Remove marcadores */
    padding: 0;
    display: flex; /* Alinha itens lado a lado (bônus, pode ser removido se não visto) */
    justify-content: center; /* Centraliza os itens (bônus) */
}

header nav li {
    margin: 0 0.5rem; /* Espaçamento entre itens do menu */
}

header nav a {
    color: white;
    text-decoration: none;
    padding: 0.6rem 1rem; /* Usando rem */
    display: block; /* Para o padding funcionar corretamente */
    transition: background-color 0.3s ease; /* Efeito suave no hover */
}

header nav a:hover {
    background-color: #34495e; /* Cor ligeiramente mais clara no hover */
    border-radius: 4px; /* Cantos arredondados no hover */
}

/* Conteúdo Principal */
main {
    padding: 1.5rem; /* Usando rem */
}

/* Artigos (Posts) */
.post {
    background-color: white;
    margin: 1.5rem auto; /* Usando rem e auto para centralizar */
    padding: 1.5rem; /* Usando rem */
    border-radius: 8px;
    box-shadow: 0 2px 5px rgba(0,0,0,0.1); /* Sombra suave */
    max-width: 800px; /* Limita a largura máxima */
}

.post h3 {
    color: #2c3e50; /* Azul escuro para títulos dos posts */
    margin-bottom: 1rem;
}

/* Imagens dentro dos posts */
.imagem-post {
    width: 100%; /* Ocupa a largura total do container */
    height: 250px; /* Altura fixa */
    object-fit: cover; /* Cobre a área sem distorcer (mantém proporção) */
    border-radius: 5px;
    margin-bottom: 1rem; /* Usando rem */
}

/* Palavras-chave (Spans dentro de parágrafos) */
.post p span {
    background-color: #e0e7ee; /* Fundo cinza-azulado claro */
    padding: 0.2rem 0.5rem; /* Usando rem */
    border-radius: 3px;
    display: inline-block; /* Para o padding/margin funcionar */
    margin-right: 0.5rem; /* Usando rem */
    font-size: 0.8rem; /* Tamanho menor para tags */
}

/* Links "Ler Mais" estilizados como botões */
.ler-mais {
    display: inline-block; /* Para poder aplicar padding/margin */
    background-color: #28a745; /* Verde */
    color: white;
    padding: 0.6rem 1rem; /* Usando rem */
    border-radius: 5px;
    text-decoration: none;
    margin-top: 1rem; /* Usando rem */
    transition: background-color 0.3s ease; /* Efeito suave no hover */
}

.ler-mais:hover {
    background-color: #218838; /* Verde mais escuro no hover */
}

/* Lista Ordenada dentro dos posts */
.post ol {
    list-style-type: decimal; /* Padrão, mas explícito */
    margin-left: 1.5rem; /* Recuo padrão para listas ordenadas */
    padding-left: 1rem;
}

.post ol li {
    margin-bottom: 0.5rem; /* Espaçamento entre itens da lista */
}

/* Seção de Contato */
#contato {
    background-color: white;
    margin: 1.5rem auto;
    padding: 1.5rem;
    border-radius: 8px;
    box-shadow: 0 2px 5px rgba(0,0,0,0.1);
    max-width: 800px;
}

#contato h2 {
    text-align: center;
    margin-bottom: 1rem;
}

#contato label {
    display: block;
    margin-bottom: 0.3rem;
    font-weight: bold;
}

#contato input[type="text"],
#contato input[type="email"] {
    width: 100%;
    padding: 0.7rem;
    margin-bottom: 1rem;
    border: 1px solid #ccc;
    border-radius: 4px;
}

#contato button {
    background-color: #007bff; /* Azul */
    color: white;
    padding: 0.8rem 1.5rem;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    font-size: 1rem;
    transition: background-color 0.3s ease;
}

#contato button:hover {
    background-color: #0056b3; /* Azul mais escuro */
}

/* Rodapé */
footer {
    background-color: #2c3e50; /* Azul escuro */
    color: white;
    text-align: center;
    padding: 1rem 0; /* Usando rem */
    font-size: 0.9rem; /* Usando rem */
    margin-top: 2rem; /* Usando rem */
}